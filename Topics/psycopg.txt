psycopg is a database driver used to connect to database when using fastapi

code to setup in ide:

import psycopg

conn = psycopg.connect(
	host = "localhost",
	port = 5432,
	dbname = "my_database_name",
	user = "postgres",
	password = "my_password"
)

cur = conn.cursor()

