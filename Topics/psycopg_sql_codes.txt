1. to get all rows and columns:

cur.execute("select * from posts_practice")
    posts = cur.fetchall()
    cur.close()
    
2. to insert values and save its changes:
    cur.execute("""
                insert into posts_practice (title, content)
                values (%s, %s)
                returning *
            """,
                (post.title, post.content)  
        )
    post = cur.fetchone()
    conn.commit()
    cur.close()
